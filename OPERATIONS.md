# Operations

The on-call owner reviews health, error-rate, latency, authentication failures, queue failures, storage capacity, and database capacity. Rotate Supabase keys through the secret manager, deploy, verify health and role smoke tests, then revoke the old key. Record every change and approver.
